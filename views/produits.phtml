<?php foreach ($products as $product) : ?>
    <article>
        <h2><?= $product['name'] ?></h2>
        <p><?= $product['description'] ?></p>
        <p><?= $product['price'] ?> €</p>
    </article>
<?php endforeach ?>